# WhatsApp Integration Configuration

# Mode: 'automated' or 'manual'
# automated: Uses whatsapp-web.js to monitor messages automatically
# manual: Traditional URL-based approach (safest, no ban risk)
WHATSAPP_MODE=automated

# Backend URL (where Flask app is running)
BACKEND_URL=http://localhost:5000

# WhatsApp group name to monitor (only messages from this group will be processed)
# IMPORTANT: Create a dedicated group for tasks to minimize ban risk
WHATSAPP_GROUP_NAME=My Tasks

# Alternative: Monitor specific chat (leave empty to use group)
# WHATSAPP_CHAT_NAME=

# Enable auto-reply with task confirmation and quick actions
AUTO_REPLY_ENABLED=true

# Rate limiting: Maximum tasks to process per hour (prevents spam detection)
MAX_TASKS_PER_HOUR=20

# Quiet hours: Don't process messages during these hours (24-hour format)
# Format: START_HOUR-END_HOUR (e.g., 23-07 means 11 PM to 7 AM)
QUIET_HOURS=23-07

# Session persistence: Save WhatsApp Web session to avoid re-scanning QR code
SAVE_SESSION=true

# Log level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=info

# ⚠️ IMPORTANT WARNINGS ⚠️
# 1. WhatsApp does NOT officially support bots or automation
# 2. Using whatsapp-web.js MAY result in account ban
# 3. Use a secondary number if possible
# 4. Monitor only ONE dedicated group to reduce detection risk
# 5. Enable rate limiting and quiet hours to appear more human
# 6. Be prepared to fall back to manual mode if banned
